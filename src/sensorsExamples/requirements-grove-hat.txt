# Hardware dependencies for sensors_rpi4_grove.py and sensors_rpi_zero_grove.py
# Install alongside the base requirements.txt:
#   pip install -r src/tatu/requirements.txt
#   pip install -r src/sensorsExamples/requirements-grove-hat.txt

# Grove Base HAT: DHT22 (D16), ADC A0/A2 (Light/Sound), Ultrasonic (D5)
seeed-python-grove>=0.2.0

# SGP41 VOC/NOX sensor (I2C 0x59) — rpi4 only
# Comunicação via smbus2 (raw I2C). Não requer biblioteca Sensirion.
# Nota: SGP41 não aparece em i2cdetect (clock stretching BCM2835);
#   workaround: dtparam=i2c_arm_baudrate=10000 em /boot/firmware/config.txt
smbus2>=0.4.0
